apiVersion: v1
kind: Service
metadata:
  name: client-svc
  namespace: uat
  labels:
    app: client-${VERSION}
spec:
  type: NodePort
  selector:
    app: client-${VERSION}
  ports:
  - port: 80
    nodePort: 30202
    protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-dp
  namespace: uat
  annotations:
    kubernetes.io/change-cause: "image name as a parameter"
    #deployment.kubernetes.io/revision: "2"
spec:
  replicas: 1
  minReadySeconds: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: client-${VERSION}
  template:
    metadata:
      labels:
        app: client-${VERSION}
    spec:
      containers:
      - name: client-ctr-${VERSION}
        image: glatrofa/dev-ops-demo:client-${VERSION}
        imagePullPolicy: Always
        ports:
        - containerPort: 80